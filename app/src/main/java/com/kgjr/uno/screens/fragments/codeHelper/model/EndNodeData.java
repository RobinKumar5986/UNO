package com.kgjr.uno.screens.fragments.codeHelper.model;

public class EndNodeData implements NodeData {

    /** false = stop the flow here, true = jump back to the start. */
    public boolean loop;
}
